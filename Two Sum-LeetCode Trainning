
//lưu ý: các bạn không cần phải làm hàm input cho bài "Two Sum" ở leet code, vì vậy bạn chỉ cần lo phần giải quyết vấn đề là chính 
class Solution {
    public int[] twoSum(int[] nums, int target) {
        //chạy vòng for để so sánh các giá trị //
        for (int i =0; i<nums.length;i++) {
            for (int j =i+1; j<nums.length;j++) {
                if (nums[i]+nums[j] == target) {
                    return new int[] {i, j};
                    // tính bằng pp brute force //
                }
            }
        }
        //trả về kết quả nếu hàm rỗng//
        return new int[] {};
    }
}
